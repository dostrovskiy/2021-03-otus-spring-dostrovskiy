spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    data: data.sql
    sql-script-encoding: UTF-8
  jpa:
    generate-ddl: false
    #generate-ddl: true
    hibernate:
      ddl-auto: none
      #ddl-auto: create-drop

    #show-sql: true
